# QTableUI v0.1.0-alpha — DRAFT / NOT YET RELEASED

Planned release type: **Open Source Preview / Alpha**

Draft started: 2026-09-02

> This file is a release-note draft. QTableUI is still in private release preparation and must not be represented as a published Alpha until the exact-revision release gates complete.

This candidate aligns QTableUI with the planned QTable v0.1.0-alpha open-source baseline.

## Verification gate

Remove `DRAFT / NOT YET RELEASED` only when:

- Frontend CI is actually assigned a runner and every frontend contract check passes on the exact reviewed main SHA;
- TypeScript + production build passes;
- secret-history scan passes;
- open-source readiness, dependency and license contracts pass;
- Docker image/runtime smoke passes;
- QTable#170 full-stack release E2E actually executes against the exact QTable + QTableUI revisions and retains revision/screenshot/trace evidence;
- QTable#139 and QTableUI#86 final release verification are complete;
- repository visibility is intentionally transitioned only after those gates; and
- the verified `v0.1.0-alpha` tag/release is created.

A workflow with `runner_id=0` and `steps=[]` is not a test execution and cannot satisfy this verification gate.

For the complete product release notes, see the QTable backend repository.
