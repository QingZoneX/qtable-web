# QTableUI normally needs no frontend secret configuration.
# Local Vite development proxies requests to http://localhost:9000.
#
# If a deployment explicitly uses a public API URL supported by the app, it must
# contain only a public endpoint. Never put API keys, JWT signing secrets,
# database passwords or encryption keys in VITE_* variables.
