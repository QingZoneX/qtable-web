registry=https://registry.npmjs.org/
# TypeScript 7 is ahead of typescript-eslint's declared peer range as of September 2026.
# Keep the locked dependency graph reproducible until upstream officially widens support.
legacy-peer-deps=true
